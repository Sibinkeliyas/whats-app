import { View, Text } from "react-native";
import React from "react";
import Camera from "@/components/users-list/Camera";

const CameraPage = () => {
  return (
    <View className="flex-1">
      <Camera />
    </View>
  );
};

export default CameraPage;
